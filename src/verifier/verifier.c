// Contains code used to verify that the parse tree is correct.
#include "verifier.h"

// Verify that the code is valid
// Valid code must be a list of functions
bool runVerifier(uba* codeTree)
{
    
}
